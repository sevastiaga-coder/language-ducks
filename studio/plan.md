# План проекта

_Заполняется на шаге /plan. Статусы: план → делаю → готово._

## Выпуск 1: Играем в «Перевод» — полноценная игра из одного этапа

| № | Что делаем | Важность | Статус | Как проверить |
|---|-----------|----------|--------|---------------|
| 1 | Игра запускается на ноутбуке, на экране виден адрес для телефонов | главное | готово | Открой терминал в папке app, набери `python3 server.py`, потом открой `http://localhost:8000/screen.html` — на экране крупно жёлтый адрес вида `192.168.x.x:8000` |
| 2 | Игроки заходят с телефонов и вводят имя — имена появляются на большом экране (Л1 + Т1) | главное | готово | Запусти сервер (`python3 server.py`), открой `screen.html` на ноутбуке, а на двух телефонах (или вкладках) — адрес без имени файла (`192.168.x.x:8000`). Введи на каждом разное имя и нажми «ИГРАТЬ» — оба имени за секунду появятся в сетке на экране ноутбука. Если ввести уже занятое имя — телефон вежливо попросит выбрать другое |
| 3 | Первый игрок нажимает «Начать» — у всех начинается игра (Т2) | главное | готово | Запусти сервер, открой `screen.html` на ноутбуке и адрес без имени файла на двух телефонах (или вкладках), войди под разными именами. У первого зашедшего внизу кнопка «НАЧАТЬ ИГРУ» (пока игроков меньше двух — кнопка серая с подсказкой), у второго — спокойная надпись, кто начнёт игру. Нажми кнопку у первого — в течение секунды экран ноутбука покажет «ИГРА НАЧАЛАСЬ», а оба телефона — «Жди задание». Обнови страницу первого игрока — кнопка остаётся у него |
| 4 | Этап «Перевод»: слово на экране, игроки печатают ответ на телефоне (Л2 + Т3) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» у первого. На ноутбуке появится крупное иностранное слово, язык и номер «Слово 1 из 5». На телефонах — поле ввода и кнопка «ОТВЕТИТЬ». Ответь с одного телефона — на экране ноутбука у его имени появится галочка, а телефон покажет «Ответ принят, жди остальных». Ответь со второго — на экране появится надпись «Все ответили!». Повторно отправить ответ нельзя — поле уже не показывается |
| 5 | Ответы всех появляются на экране, правильный подсвечен (Л3) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ». Ответь с одного телефона верно, с другого — неверно. Через пару секунд после второго ответа экран ноутбука сам переключится: появятся карточки обоих игроков с их ответами, верная — с жёлтой рамкой и подписью «ВЕРНО», рядом крупно виден правильный перевод. На каждом телефоне в этот момент видно лично: «Ты угадал!» или «Мимо» |
| 6 | За правильный ответ начисляются очки, вопросы идут один за другим | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ». Ответь один раз верно, другой раз неверно — на экране ноутбука рядом с каждым ответом появится «+100» или «+0», а внизу — таблица очков с подсветкой лидера. На телефоне после раскрытия видно личное начисление и «Твои очки: …». Через несколько секунд само включается следующее слово — так 5 раз подряд, без единого нажатия на ноутбуке. После пятого слова на экране появляется крупная надпись «ИГРА ОКОНЧЕНА» |
| 7 | Финальная таблица: места и победитель (Л7) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» и пройди все 5 слов. После последнего слова на экране ноутбука появится настоящий финал: сверху крупно имя победителя и его очки, ниже таблица всех игроков по местам. Если у обоих одинаковые очки — оба будут показаны как «ПОБЕДИТЕЛИ» с местом 1, без выдуманного случайного победителя. На телефоне каждого видно его место и очки |
| 7.1 | Кнопка «Сыграть ещё раз» — ведущий начинает новую партию, не теряя игроков | хорошо бы | готово | Дойди до финала. На телефоне ведущего внизу — кнопка «СЫГРАТЬ ЕЩЁ РАЗ», у остальных — надпись, что новую игру начнёт ведущий. Нажми кнопку — экран ноутбука и все телефоны вернутся в лобби с теми же именами, но с очками 0. Нажми «НАЧАТЬ ИГРУ» у ведущего — пойдёт новая партия с другими словами |
| 8 | Игра запускается двойным кликом, без терминала | хорошо бы | готово | Найди в папке проекта (не в app/) файл `Играть.command` и дважды кликни. Откроется тёмное окно и следом сам браузер с экраном игры — в окне крупно написан адрес для телефонов вида `192.168.x.x:8000`. Если до этого игра уже была открыта в другом окне и его забыли закрыть — новый запуск сам закроет старое и откроется заново, без ошибок. Чтобы закончить игру — просто закрой это окно |

**После выпуска 1** можно сесть компанией и по-настоящему сыграть: телефоны подключены, слова переводятся, очки считаются, победитель есть.

## Выпуск 2: «Обманка» — второй этап

| № | Что делаем | Важность | Статус | Как проверить |
|---|-----------|----------|--------|---------------|
| 9 | «Обманка»: редкое слово на экране, каждый придумывает фальшивый перевод (Л4 + Т3) | главное | план | Все ввели выдумки — на ноутбуке видно, кто уже придумал |
| 10 | Голосование: все варианты вперемешку с настоящим, за свой голосовать нельзя (Л5 + Т4) | главное | план | На телефоне список кнопок, свой вариант серый и не нажимается |
| 11 | Итог «Обманки»: раскрывается настоящий перевод, очки за угадывание и за обман (Л6) | главное | план | Видно, кто за что голосовал, и у кого сколько прибавилось |
| 12 | Оба этапа идут подряд в одной игре: «Перевод» → «Обманка» → финал | главное | план | Одна игра от начала до конца проходит через оба этапа |

---

## Что сломано

_Сюда tester и босс записывают баги. Формат: что сделал → что ожидал → что случилось._

| Что не так | Когда появляется | Статус |
|-----------|------------------|--------|
| Спрятанные экраны оставались видны: надписи «Игра уже идёт» и «ИГРА НАЧАЛАСЬ» висели поверх рабочего экрана | Всегда, на всех экранах — атрибут `hidden` перебивался правилом `display: flex` в стилях | починено |
| Один телефон превращался в двух игроков на экране | Игрок ввёл имя → сыграл → вернулся на экран входа и ввёл другое имя → нажал «ИГРАТЬ». Причина: страница входа при повторном заходе не передавала серверу сохранённый id, поэтому сервер каждый раз заводил нового игрока вместо переименования старого | починено |
| Знакомый телефон мог сменить имя игрока прямо посреди игры | Двое зашли, игра запущена (фаза playing). Один из уже играющих телефонов заново прислал своё имя, но другое, вместе со своим сохранённым id. Ожидал: игрок просто возвращается в игру. Получилось: сервер переименовывал игрока в любой фазе игры, и имя на большом экране менялось прямо во время партии. Починил: переименование в `handle_join` теперь разрешено только пока фаза «lobby»; если игра уже идёт, новое имя игнорируется и знакомый телефон возвращается в игру под прежним именем без ошибки. Ведущий по-прежнему остаётся ведущим при обновлении страницы | починено |
| Игрок, зашедший вовремя, после старта видел «Игра уже идёт» | Проверил два окна одного браузера как два разных телефона (общее хранилище playerId). Ожидал: два независимых игрока. Получилось: второе окно при входе перезаписывало сохранённый id первого и подменяло собой его игрока — оба окна путались, кто есть кто, а второй игрок фактически не заводился отдельно. Починил: id игрока теперь хранится в `sessionStorage` вместо `localStorage` — у каждой вкладки/окна своё хранилище, поэтому два окна больше не путают игроков, а обновление страницы игрока по-прежнему не теряет его. Ведущий при обновлении страницы остаётся ведущим — проверил отдельно. Отдельно нашёл ещё один путь к точному тексту «Игра уже идёт» для игрока, который реально успел зайти: если во время тестирования сервер перезапускали (`python3 server.py` заново) уже после того, как чей-то телефон зашёл, то этот телефон помнит свой id, а новый сервер о нём не знает — при следующем заходе (уже после того как ведущий заново нажал «Начать») телефон получает то же самое сообщение. Это не то же самое, что баг с двумя окнами, и не чинил — тут нужно решение, как вести себя при потере памяти сервера (например, отдельное сообщение вместо «Игра уже идёт»), а не просто починка кода | починено (хранилище) + отдельная находка про перезапуск сервера — нужно решение |
| Карточки ответов на большом экране мигали: появлялись и исчезали | На Л3 сразу после раскрытия ответов, пока экран ждал перехода к следующему слову. Причина: экран опрашивает сервер раз в секунду и каждый раз пересоздавал все карточки заново, из-за чего анимация появления запускалась с нуля на каждый опрос. Починил: экран теперь запоминает, какие данные (слово + ответы) уже нарисованы, и трогает DOM только когда они реально изменились. Заодно тем же способом починил список игроков в лобби и список галочек «кто ответил» на Л2 — они пересоздавались так же без нужды | починено |

---

## Правки с приёмки выпуска 1

- Язык слова теперь заметно виден и на экране задания (Л2), и на экране показа ответов (Л3) — выполнено
- Слова в `words.py` пересмотрены: убраны слишком заумные и «непереводимые» (der Kummerspeck, la sobremesa, serendipity, вавёрка), добавлены более узнаваемые. У каждого слова есть запасные варианты перевода — выполнено
- Слова в игре повторялись по смыслу (бабочка/зеркало выпадали по несколько раз за партию). Все 20 слов в `words.py` пересобраны так, чтобы каждое было про своё понятие: животные, еда, погода, вещи из дома, части тела, время, эмоции — ни одна тема не дублируется. Выбор пятёрки слов на партию (`pick_round_words` в `server.py`) теперь берёт по одному слову из каждого языка — так слова в партии гарантированно из разных языков (было — иногда четыре испанских подряд) и не могут случайно повторить один и тот же перевод — выполнено

## Правки после проверки дизайнером (выпуск 1)

- Экран показа ответов (Л3) не помещался на 1440×900 при 5 и 10 игроках. Карточки теперь раскладываются в два столбца от пятого игрока, таблица очков стоит рядом с карточками, а не под ними, содержимое экрана прижато к верху. Проверено на 5 и на 10 игроках — без прокрутки — выполнено
- Шрифты на экране ноутбука увеличены для взгляда с двух метров: ответ игрока 34px, «+100» 30px, «ВЕРНО» 20px, строка таблицы очков 24px, место в финальной таблице 24px, «Ответили 3 из 5» 26px. Значения обновлены и в `studio/style.md` — выполнено
- Кнопка «ИГРАТЬ» на входе (Т1) снова нажимается при пустом имени, а под полем появляется голубая подсказка — выполнено
- Логотип LANGUAGE DUCKS убран с игровых экранов: на ноутбуке (Л2–Л7) он теперь маленький (22px) без свечения, на телефоне (везде кроме входа) убран совсем — выполнено
- Класс `.join-hint` был задублирован (ноутбук 18px и телефон 16px), из-за чего подпись под адресом на Л1 всегда рендерилась мельче задуманного. Телефонный класс переименован в `.field-hint`, классы больше не конфликтуют — выполнено
- На экране ожидания (Т2) вернулась вторая строка состояния («Ждём остальных» / «Скоро задание»), первая строка сделана крупнее и заметнее — выполнено
- Поле ответа на телефоне (Т3) теперь получает фокус автоматически при появлении нового слова, не перебивая набор текста — выполнено
- Показ ответов на ноутбуке длится 9 секунд вместо 6 — выполнено
- Средняя из трёх голубых строк результата раунда на телефоне («Твои очки: …») приглушена цветом `--text-dim`, чтобы не сливаться с соседними строками — выполнено
- В `studio/style.md` дописаны два решения: сплошная жёлтая заливка адреса на Л1 и финальная подпись гостя на телефоне «Ведущий может начать новую игру со своего телефона» (текст в игре приведён в соответствие) — выполнено

---

**Важность:** главное (без этого проекта нет) · хорошо бы (сильно улучшает) · потом (если останется время)
