# План проекта

_Заполняется на шаге /plan. Статусы: план → делаю → готово._

## Выпуск 1: Играем в «Перевод» — полноценная игра из одного этапа (принят боссом 2026-08-25)

| № | Что делаем | Важность | Статус | Как проверить |
|---|-----------|----------|--------|---------------|
| 1 | Игра запускается на ноутбуке, на экране виден адрес для телефонов | главное | готово | Открой терминал в папке app, набери `python3 server.py`, потом открой `http://localhost:8000/screen.html` — на экране крупно жёлтый адрес вида `192.168.x.x:8000` |
| 2 | Игроки заходят с телефонов и вводят имя — имена появляются на большом экране (Л1 + Т1) | главное | готово | Запусти сервер (`python3 server.py`), открой `screen.html` на ноутбуке, а на двух телефонах (или вкладках) — адрес без имени файла (`192.168.x.x:8000`). Введи на каждом разное имя и нажми «ИГРАТЬ» — оба имени за секунду появятся в сетке на экране ноутбука. Если ввести уже занятое имя — телефон вежливо попросит выбрать другое |
| 3 | Первый игрок нажимает «Начать» — у всех начинается игра (Т2) | главное | готово | Запусти сервер, открой `screen.html` на ноутбуке и адрес без имени файла на двух телефонах (или вкладках), войди под разными именами. У первого зашедшего внизу кнопка «НАЧАТЬ ИГРУ» (пока игроков меньше двух — кнопка серая с подсказкой), у второго — спокойная надпись, кто начнёт игру. Нажми кнопку у первого — в течение секунды экран ноутбука покажет «ИГРА НАЧАЛАСЬ», а оба телефона — «Жди задание». Обнови страницу первого игрока — кнопка остаётся у него |
| 4 | Этап «Перевод»: слово на экране, игроки печатают ответ на телефоне (Л2 + Т3) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» у первого. На ноутбуке появится крупное иностранное слово, язык и номер «Слово 1 из 5». На телефонах — поле ввода и кнопка «ОТВЕТИТЬ». Ответь с одного телефона — на экране ноутбука у его имени появится галочка, а телефон покажет «Ответ принят, жди остальных». Ответь со второго — на экране появится надпись «Все ответили!». Повторно отправить ответ нельзя — поле уже не показывается |
| 5 | Ответы всех появляются на экране, правильный подсвечен (Л3) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ». Ответь с одного телефона верно, с другого — неверно. Через пару секунд после второго ответа экран ноутбука сам переключится: появятся карточки обоих игроков с их ответами, верная — с жёлтой рамкой и подписью «ВЕРНО», рядом крупно виден правильный перевод. На каждом телефоне в этот момент видно лично: «Ты угадал!» или «Мимо» |
| 6 | За правильный ответ начисляются очки, вопросы идут один за другим | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ». Ответь один раз верно, другой раз неверно — на экране ноутбука рядом с каждым ответом появится «+100» или «+0», а внизу — таблица очков с подсветкой лидера. На телефоне после раскрытия видно личное начисление и «Твои очки: …». Через несколько секунд само включается следующее слово — так 5 раз подряд, без единого нажатия на ноутбуке. После пятого слова на экране появляется крупная надпись «ИГРА ОКОНЧЕНА» |
| 7 | Финальная таблица: места и победитель (Л7) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» и пройди все 5 слов. После последнего слова на экране ноутбука появится настоящий финал: сверху крупно имя победителя и его очки, ниже таблица всех игроков по местам. Если у обоих одинаковые очки — оба будут показаны как «ПОБЕДИТЕЛИ» с местом 1, без выдуманного случайного победителя. На телефоне каждого видно его место и очки |
| 7.1 | Кнопка «Сыграть ещё раз» — ведущий начинает новую партию, не теряя игроков | хорошо бы | готово | Дойди до финала. На телефоне ведущего внизу — кнопка «СЫГРАТЬ ЕЩЁ РАЗ», у остальных — надпись, что новую игру начнёт ведущий. Нажми кнопку — экран ноутбука и все телефоны вернутся в лобби с теми же именами, но с очками 0. Нажми «НАЧАТЬ ИГРУ» у ведущего — пойдёт новая партия с другими словами |
| 8 | Игра запускается двойным кликом, без терминала | хорошо бы | готово | Найди в папке проекта (не в app/) файл `Играть.command` и дважды кликни. Откроется тёмное окно и следом сам браузер с экраном игры — в окне крупно написан адрес для телефонов вида `192.168.x.x:8000`. Если до этого игра уже была открыта в другом окне и его забыли закрыть — новый запуск сам закроет старое и откроется заново, без ошибок. Чтобы закончить игру — просто закрой это окно |

**После выпуска 1** можно сесть компанией и по-настоящему сыграть: телефоны подключены, слова переводятся, очки считаются, победитель есть.

## Выпуск 2: «Обманка» — второй этап (принят боссом 2026-08-26)

| № | Что делаем | Важность | Статус | Как проверить |
|---|-----------|----------|--------|---------------|
| 9 | «Обманка»: редкое слово на экране, каждый придумывает фальшивый перевод (Л4 + Т3) | главное | готово | Запусти сервер, зайди двумя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» и пройди этап «Перевод» (5 слов) и заставку — начнётся «Обманка». На экране ноутбука появится редкое слово, его язык и подпись «Придумай перевод, чтобы все поверили». На телефонах — поле ввода и кнопка «ПРИДУМАТЬ». Придумай что-нибудь с одного телефона — на экране у имени появится галочка, телефон покажет «Придумка принята, жди остальных». Если случайно ввести настоящий перевод слова — телефон вежливо скажет «Ты угадал настоящий перевод! Придумай обманку» и даст ввести другое. Если два игрока придумают одно и то же — второму предложат придумать другое. Когда придумали все — на экране появится «Все придумали!» |
| 10 | Голосование: все варианты вперемешку с настоящим, за свой голосовать нельзя (Л5 + Т4) | главное | готово | Запусти сервер, зайди тремя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» и дойди до «Обманки» (после «Перевода» и заставки). Придумай выдумку с каждого телефона — через пару секунд после последней экран ноутбука и все телефоны сами переключатся на голосование: пронумерованный список вариантов (все выдумки вперемешку с настоящим переводом), номера везде одинаковые. На каждом телефоне свой собственный вариант виден в списке, но серый и не нажимается — остальные нажимаются. Нажми на любой чужой вариант — телефон покажет «Голос принят, жди остальных», а на экране ноутбука у имени этого игрока появится галочка «проголосовал». Повторно нажать нельзя — кнопки уже не видно. Когда проголосуют все — на экране ноутбука появится надпись «Все проголосовали!». Отдельно проверено на двух игроках — вариантов всего три (своя выдумка недоступна, доступны выдумка второго игрока и правда) |
| 11 | Итог «Обманки»: раскрывается настоящий перевод, очки за угадывание и за обман (Л6) | главное | готово | Запусти сервер, зайди тремя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ» и дойди до «Обманки» (после «Перевода» и заставки). Придумай выдумку с каждого телефона, потом проголосуй с каждого за любой чужой вариант. Через пару секунд после последнего голоса экран ноутбука покажет итог: настоящий перевод выделен жёлтой рамкой с подписью «НАСТОЯЩИЙ», у каждого варианта видно, кто его придумал и кто за него проголосовал, справа — начисленные очки (+150 у настоящего, +100 у выдумки за каждого, кто попался). На каждом телефоне видно лично: угадал ты или нет (и кто тебя обманул, если да), сколько получил за угадывание, сколько за обманутых и сколько очков всего. Через 9 секунд само включится следующее слово «Обманки» — так все 3 слова, а после третьего — общий финал. «Сыграть ещё раз» работает, слова новой партии другие. Проверено отдельно на 10 игроках (11 вариантов) — экран не уезжает за край |
| 12 | Оба этапа идут подряд в одной игре: «Перевод» → «Обманка» → финал | главное | готово | Запусти сервер, зайди тремя телефонами (вкладками), войди под разными именами, нажми «НАЧАТЬ ИГРУ». Партия идёт с «Перевода» — пройди все 5 слов. После пятого слова на экране ноутбука на 6 секунд появится заставка «ЭТАП 2 · ОБМАНКА» с фразой «Придумай, что значит слово, и обмани остальных», а телефоны в это время покажут «Смотри на большой экран». Заставка сама уйдёт в первое слово «Обманки» — пройди все 3 слова. После третьего — общий финал, очки в таблице сложены за оба этапа вместе. «Сыграть ещё раз» запускает новую партию заново с «Перевода», слова снова другие |

## Выпуск 3: Игра через интернет — играем по ссылке, не только по домашнему Wi-Fi

| № | Что делаем | Важность | Статус | Как проверить |
|---|-----------|----------|--------|---------------|
| 13 | Сервер работает и дома (двойной клик), и в интернете (Railway) — сам понимает, в каком он режиме, и показывает правильный адрес | главное | готово | Дома: открой `Играть.command` двойным кликом — всё как раньше, на экране крупный адрес вида `192.168.x.x:8000`. В интернете (после публикации на Railway, когда родитель заведёт проект): открой ссылку на большом экране — вместо домашнего адреса будет показан публичный адрес сайта (он длиннее, но помещается на экран и читается), и с телефона по нему заходят так же, как раньше |
| 14 | Игроки, которые закрыли вкладку, сами исчезают из игры; если ушли все — игра сама возвращается в чистое лобби; ведущий не теряется | главное | готово | Зайди двумя телефонами (вкладками), войди под разными именами. Закрой вкладку одного игрока и не трогай её полминуты — на большом экране он исчезнет из списка сам. Если закрыть все вкладки — игра через полминуты станет чистым лобби (можно проверить, открыв большой экран заново). Если ушёл именно ведущий (первый зашедший) — кнопка «НАЧАТЬ ИГРУ» сама появится у следующего игрока. Если игрок ушёл прямо посреди партии (не ответив на слово) — игра не виснет и идёт дальше, как только ответили все оставшиеся. Игрок, который просто думает над ответом, не трогая телефон, никуда не пропадает — экран всё это время сам стучится на сервер |

---

## Что сломано

_Сюда tester и босс записывают баги. Формат: что сделал → что ожидал → что случилось._

| Что не так | Когда появляется | Статус |
|-----------|------------------|--------|
| Спрятанные экраны оставались видны: надписи «Игра уже идёт» и «ИГРА НАЧАЛАСЬ» висели поверх рабочего экрана | Всегда, на всех экранах — атрибут `hidden` перебивался правилом `display: flex` в стилях | починено |
| Один телефон превращался в двух игроков на экране | Игрок ввёл имя → сыграл → вернулся на экран входа и ввёл другое имя → нажал «ИГРАТЬ». Причина: страница входа при повторном заходе не передавала серверу сохранённый id, поэтому сервер каждый раз заводил нового игрока вместо переименования старого | починено |
| Знакомый телефон мог сменить имя игрока прямо посреди игры | Двое зашли, игра запущена (фаза playing). Один из уже играющих телефонов заново прислал своё имя, но другое, вместе со своим сохранённым id. Ожидал: игрок просто возвращается в игру. Получилось: сервер переименовывал игрока в любой фазе игры, и имя на большом экране менялось прямо во время партии. Починил: переименование в `handle_join` теперь разрешено только пока фаза «lobby»; если игра уже идёт, новое имя игнорируется и знакомый телефон возвращается в игру под прежним именем без ошибки. Ведущий по-прежнему остаётся ведущим при обновлении страницы | починено |
| Игрок, зашедший вовремя, после старта видел «Игра уже идёт» | Проверил два окна одного браузера как два разных телефона (общее хранилище playerId). Ожидал: два независимых игрока. Получилось: второе окно при входе перезаписывало сохранённый id первого и подменяло собой его игрока — оба окна путались, кто есть кто, а второй игрок фактически не заводился отдельно. Починил: id игрока теперь хранится в `sessionStorage` вместо `localStorage` — у каждой вкладки/окна своё хранилище, поэтому два окна больше не путают игроков, а обновление страницы игрока по-прежнему не теряет его. Ведущий при обновлении страницы остаётся ведущим — проверил отдельно. Отдельно нашёл ещё один путь к точному тексту «Игра уже идёт» для игрока, который реально успел зайти: если во время тестирования сервер перезапускали (`python3 server.py` заново) уже после того, как чей-то телефон зашёл, то этот телефон помнит свой id, а новый сервер о нём не знает — при следующем заходе (уже после того как ведущий заново нажал «Начать») телефон получает то же самое сообщение. Это не то же самое, что баг с двумя окнами, и не чинил — тут нужно решение, как вести себя при потере памяти сервера (например, отдельное сообщение вместо «Игра уже идёт»), а не просто починка кода | починено (хранилище) + отдельная находка про перезапуск сервера — нужно решение |
| Карточки ответов на большом экране мигали: появлялись и исчезали | На Л3 сразу после раскрытия ответов, пока экран ждал перехода к следующему слову. Причина: экран опрашивает сервер раз в секунду и каждый раз пересоздавал все карточки заново, из-за чего анимация появления запускалась с нуля на каждый опрос. Починил: экран теперь запоминает, какие данные (слово + ответы) уже нарисованы, и трогает DOM только когда они реально изменились. Заодно тем же способом починил список игроков в лобби и список галочек «кто ответил» на Л2 — они пересоздавались так же без нужды | починено |
| Итог «Обманки» (Л6) в окне ≈757px (не на весь экран) при 10 игроках, если все десять одновременно придумывают выдумки под сорок символов (максимум ввода на телефоне) — самый тяжёлый случай из возможных — карточкам чуть-чуть не хватает высоты, появляется небольшая прокрутка. На полном экране (900px) почти сходится (разница мельче одной карточки). Проверил на разных партиях: при обычной длине выдумок (не под завязку) помещается без прокрутки и на 900, и на 757. Не дочинил — дальше можно либо смириться (это самый край, редкий на практике), либо укоротить лимит ввода выдумки на телефоне (сейчас 40 символов), либо принять, что этот экран смотрят в полноэкранном режиме | не починено — редкий крайний случай, нужно решение |
| Сервер отдавал любой файл с компьютера, не только из папки игры (`serve_file` не проверял, что путь остаётся внутри `app/`) — с телефона в том же Wi-Fi запрос вида `/../../../../etc/passwd` или `/../CLAUDE.md` возвращал файл. Заодно `/words.py` отдавался как есть и показывал все правильные ответы игры — готовое читерство. Проверено перед починкой: `/../CLAUDE.md`, `/../../etc/passwd`, `/words.py` — все отвечали 200. Починил: `serve_file` теперь раздаёт только `.html`, `.css`, `.js`, `.ico` и всегда проверяет, что итоговый путь не выходит за пределы `app/` — всё остальное 404. Проверено после починки: `/`, `/screen.html`, `/style.css`, `/app.js` работают как раньше; `/../CLAUDE.md`, `/../../etc/passwd`, `/words.py`, `/server.py` — все 404; партия тремя игроками до конца прошла без сбоев | починено |

---

## Правки с приёмки выпуска 1

- Язык слова теперь заметно виден и на экране задания (Л2), и на экране показа ответов (Л3) — выполнено
- Слова в `words.py` пересмотрены: убраны слишком заумные и «непереводимые» (der Kummerspeck, la sobremesa, serendipity, вавёрка), добавлены более узнаваемые. У каждого слова есть запасные варианты перевода — выполнено
- Слова в игре повторялись по смыслу (бабочка/зеркало выпадали по несколько раз за партию). Все 20 слов в `words.py` пересобраны так, чтобы каждое было про своё понятие: животные, еда, погода, вещи из дома, части тела, время, эмоции — ни одна тема не дублируется. Выбор пятёрки слов на партию (`pick_round_words` в `server.py`) теперь берёт по одному слову из каждого языка — так слова в партии гарантированно из разных языков (было — иногда четыре испанских подряд) и не могут случайно повторить один и тот же перевод — выполнено

## Правки с приёмки выпуска 1 (второй заход)

- «Сыграть ещё раз» подсовывало те же самые слова: `pick_round_words` не помнило прошлую партию. Теперь сервер запоминает слова только что законченной партии (`previous_words` в `handle_restart`) и при выборе новой пятёрки для каждого языка сначала пробует взять слово из трёх оставшихся (не из прошлой партии), и только если выбрать не из чего — берёт из всех четырёх, без ошибок. Проверено: сыграл партию, «Сыграть ещё раз», сыграл вторую — пересечений слов между партиями нет — выполнено
- На последнем слове партии внизу экрана ноутбука висела подпись «Следующее слово через несколько секунд», хотя дальше идёт финал. Теперь на последнем слове (когда `wordIndex` равен `wordTotal`) подпись меняется на честную «Считаем итоги», тем же шрифтом и цветом, что и раньше. Проверено на живой партии — выполнено

## Правки после проверки дизайнером (выпуск 1)

- Экран показа ответов (Л3) не помещался на 1440×900 при 5 и 10 игроках. Карточки теперь раскладываются в два столбца от пятого игрока, таблица очков стоит рядом с карточками, а не под ними, содержимое экрана прижато к верху. Проверено на 5 и на 10 игроках — без прокрутки — выполнено
- Шрифты на экране ноутбука увеличены для взгляда с двух метров: ответ игрока 34px, «+100» 30px, «ВЕРНО» 20px, строка таблицы очков 24px, место в финальной таблице 24px, «Ответили 3 из 5» 26px. Значения обновлены и в `studio/style.md` — выполнено
- Кнопка «ИГРАТЬ» на входе (Т1) снова нажимается при пустом имени, а под полем появляется голубая подсказка — выполнено
- Логотип LANGUAGE DUCKS убран с игровых экранов: на ноутбуке (Л2–Л7) он теперь маленький (22px) без свечения, на телефоне (везде кроме входа) убран совсем — выполнено
- Класс `.join-hint` был задублирован (ноутбук 18px и телефон 16px), из-за чего подпись под адресом на Л1 всегда рендерилась мельче задуманного. Телефонный класс переименован в `.field-hint`, классы больше не конфликтуют — выполнено
- На экране ожидания (Т2) вернулась вторая строка состояния («Ждём остальных» / «Скоро задание»), первая строка сделана крупнее и заметнее — выполнено
- Поле ответа на телефоне (Т3) теперь получает фокус автоматически при появлении нового слова, не перебивая набор текста — выполнено
- Показ ответов на ноутбуке длится 9 секунд вместо 6 — выполнено
- Средняя из трёх голубых строк результата раунда на телефоне («Твои очки: …») приглушена цветом `--text-dim`, чтобы не сливаться с соседними строками — выполнено
- В `studio/style.md` дописаны два решения: сплошная жёлтая заливка адреса на Л1 и финальная подпись гостя на телефоне «Ведущий может начать новую игру со своего телефона» (текст в игре приведён в соответствие) — выполнено
- Очки за угадывание настоящего перевода в «Обманке» снижены с 200 до 150 (очки за обманутых — по-прежнему 100 за каждого) — выполнено
- Настоящий перевод в списке для голосования и в итоге «Обманки» отличался по написанию от придумок игроков (телефон автоматически делает первую букву заглавной, а перевод в словаре был с маленькой) — теперь все варианты на экране приводятся к одному виду: первая буква заглавная, остальное как есть. Сравнение ответов (совпадение с правдой, совпадение придумок между собой) по-прежнему не зависит от регистра — выполнено
- В `words.py` сокращены слишком длинные переводы «Обманки», которые выдавали правду длиной фразы: «крупный снегопад без ветра» → «безветренный снегопад», «обманчиво хорошая погода за окном» → «обманчивая погода», «отгул из-за солнечной погоды» → «солнечный отгул», «пить дома в белье» → «пьянка в трусах» — выполнено

## Правки «за столом, не на весь экран» (10 игроков, обычное окно ~757px)

- Голосование (Л5): шапка сжата в одну-две строки, номер варианта крупнее (кружок 44px, цифра 24px), список уходит в два столбца от 7 вариантов и в три от 10 — помещается на 1440×900 и в окне ≈757px при 10 игроках — выполнено
- Финальная таблица (Л7): от 8 игроков — два столбца, панель и строки таблицы плотнее, имя победителя (`.word`) остаётся самым крупным элементом. Отдельно починено: при ничьей на 1 месте у нескольких игроков сразу имя-список («Аня и Боря и Вася») не помещалось в шрифт «во весь рост» — теперь при нескольких победителях шрифт заметно скромнее (класс `.winner-name.multi`), но имя всё равно крупнее всего остального — выполнено
- Итог «Обманки» (Л6): настоящий перевод — отдельная широкая карточка над сеткой (38px), у каждой карточки выдумки одинаковая структура (автор ≥26px своей строкой, попавшиеся ≥22px второй строкой, начисление рядом с текстом), список попавшихся показывает всех и сокращается только после пяти имён. Текст выдумки ограничен двумя строками (макс. длина ввода на телефоне и так 40 символов), панель и карточки уплотнены, ширина экрана для Л5/Л6 расширена до 1380px — при 10 игроках и выдуманных фразах под сорок символов сходится почти вплотную к 900px и заметно ближе к 757px, чем было. Проверено: короткие фразы — помещается полностью; самый нагруженный стресс-тест (все 10 игроков одновременно пишут выдумки под сорок символов) в окне 757px всё ещё требует небольшой прокрутки — см. «Что сломано» — выполнено (частично, см. ниже)
- Придумывание (Л4): счётчик «Слово 1 из 3» добавлен, экран центрирован по вертикали — выполнено
- Мгновенная реакция на голосование (Т4): нажатая кнопка сразу подсвечивается жёлтым, остальные гаснут, не дожидаясь ответа сервера; текст кнопки 22px — выполнено
- Личный итог «Обманки» (Т5): добавлена строка «Ты на N месте» (обычным цветом), «Твои очки» приглушены; «За обманутых» заменено на текст с именами («Пётр клюнул на твою выдумку», при большом числе — счётчик) — выполнено

## Правки после приёмки выпуска 2

- Кнопки вариантов на голосовании (Т4) сливались с фоном на телефоне. Теперь фон кнопки заметно светлее (голубая подсветка поверх карточки) и рамка сплошная голубая вместо тусклой — явно читается как кнопка. Свой собственный вариант остался приглушённым и не нажимается — стиль как у неактивной главной кнопки (прозрачный фон, тусклая рамка и текст). `studio/style.md` обновлён — выполнено
- Слова заканчивались слишком быстро. `WORDS` расширен с 20 до 50 (по 10 на английский, немецкий, испанский, украинский, белорусский), `TRICK_WORDS` — с 12 до 30 (по 10 на финский, норвежский, исландский). Все 50 значений в «Переводе» разные, у каждого слова есть запасные варианты. Проверено: 10 партий подряд без повторов слов между соседними партиями, полная партия тремя игроками до финала — выполнено

## Правки после полного теста игры на сервере

- Показ результатов держался всего 9 секунд — не успевали рассмотреть. `NEXT_WORD_DELAY_SECONDS` поднят до 20 секунд, действует и на «Переводе», и на «Обманке» (это один и тот же таймер для обоих этапов) — выполнено
- Правильный ответ на показе ответов «Перевода» был мелкой подписью. Теперь это отдельная крупная карточка сверху — тот же приём, что и «Настоящий перевод» на итоге «Обманки». Проверено на 10 игроках — экран по-прежнему помещается без прокрутки — выполнено
- Добавлено кряканье утки на большом экране: звучит один раз за каждого игрока, у которого появилась галочка (ответ в «Переводе», придумка или голос в «Обманке»), не повторяется при опросах сервера. Звук синтезируется в браузере (Web Audio API), файлов нет. В углу экрана — кнопка звука (запоминает состояние в браузере, честно показывает «выключено», пока не кликнули хотя бы раз — так требуют браузеры). Проверено скриптом клика по кнопке: звук по-настоящему разрешается после первого клика, повторные клики переключают вкл/выкл и запоминаются; и сценарием игры — счётчик «крякований» растёт ровно на 1 за каждого ответившего и не растёт дальше при повторных опросах — выполнено

---

**Важность:** главное (без этого проекта нет) · хорошо бы (сильно улучшает) · потом (если останется время)
